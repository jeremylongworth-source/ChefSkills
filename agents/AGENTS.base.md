Skills files are available at `$CODEX_HOME/skills` or `%USERPROFILE%\.codex\skills`.

When a user request matches a local skill, prefer using the relevant skill instead of answering from memory alone. Read the skill's `SKILL.md` first, then load referenced files only when needed.

Food safety overrides culinary preference. When a safety issue is plausible, activate `food-safety` and give conservative guidance.
